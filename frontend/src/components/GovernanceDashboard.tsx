import { Button, Card } from "@/lib/design-system";
const actions = ["Asset whitelist change", "Oracle feed or staleness change", "Fee parameter change", "Emergency pause"];
export function GovernanceDashboard() { return <div className="mt-8 grid gap-6 lg:grid-cols-[1fr_.9fr]"><Card><p className="eyebrow">Signer state</p><h2 className="mt-3 font-display text-2xl uppercase">Governance not connected</h2><p className="mt-3 leading-7 text-orange-50">Standard changes require two of three authorized governance signers followed by a 24-hour timelock. Emergency pause is a distinct one-of-three authority with no timelock. Neither pathway is active before governance program deployment.</p><Button disabled className="mt-7">Connect governance signer</Button></Card><Card><p className="eyebrow">Action policy</p><ul className="mt-5 space-y-3">{actions.map((action, index) => <li key={action} className="flex items-center justify-between border-b border-amber/10 pb-3 font-mono text-xs uppercase tracking-wider"><span>{action}</span><span className="text-amber">{index === 3 ? "1-of-3 emergency" : "2-of-3 + 24h"}</span></li>)}</ul></Card></div>; }
