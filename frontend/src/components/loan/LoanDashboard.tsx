import { Card } from "@/lib/design-system";

export function LoanDashboard() {
  return <div className="mt-8 grid gap-5 xl:grid-cols-3"><Card className="xl:col-span-2"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="eyebrow">Payment status</p><h2 className="mt-2 font-display text-2xl uppercase">Awaiting verified on-chain state</h2></div><span className="border border-amber/30 bg-amber/10 px-3 py-2 font-mono text-xs uppercase tracking-widest text-amber">Data unavailable</span></div><div className="mt-7 h-3 overflow-hidden border border-amber/20 bg-ink"><div className="h-full w-0 bg-amber" /></div><div className="mt-3 flex justify-between font-mono text-[11px] uppercase tracking-widest text-orange-50/70"><span>Current payment window</span><span>— days remaining</span></div></Card><Card><p className="eyebrow">Collateral bridge</p><p className="mt-3 font-display text-xl uppercase">Not funded</p><p className="mt-3 text-sm leading-6 text-orange-50">Once funded, this panel shows the tBTC or zBTC bridge/token backing the loan and a verified explorer link.</p></Card><Metric title="Collateral locked" value="— BTC" helper="Fresh BTC/USD value required" /><Metric title="Outstanding balance" value="—" helper="USDC or USDT" /><Metric title="Payments made" value="— / —" helper="Fixed monthly schedule" /><Card className="xl:col-span-3"><p className="eyebrow">LTV health</p><div className="mt-5 h-4 overflow-hidden border border-amber/20 bg-ink"><div className="h-full w-0 bg-amber" /></div><div className="mt-3 flex justify-between font-mono text-[11px] uppercase tracking-widest text-orange-50/70"><span>0%</span><span className="text-amber">Warning: 70%</span><span className="text-orange">Liquidation threshold set by asset risk parameters</span></div><p className="mt-5 text-sm leading-6 text-orange-50">Price-dependent information intentionally remains unavailable until a fresh BTC/USD oracle update and deployed program state can be verified. The product must fail closed rather than show or act on stale values.</p></Card></div>;
}
function Metric({ title, value, helper }: { title: string; value: string; helper: string }) { return <Card><p className="eyebrow">{title}</p><p className="mt-3 font-display text-3xl text-amber">{value}</p><p className="mt-2 text-xs text-orange-50/70">{helper}</p></Card>; }
