import { Button, Card } from "@/lib/design-system";

/** The proposal review contains values only: no free-text conversation surface exists. */
export function ProposalReview() {
  return <Card className="mt-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="eyebrow">Screen B4 // incoming proposals</p><h2 className="mt-2 font-display text-2xl uppercase">Compare structured terms</h2></div><span className="border border-amber/30 px-3 py-2 font-mono text-[10px] uppercase tracking-widest text-amber">Wallet connection required</span></div><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[620px] border-collapse text-left"><thead className="border-y border-amber/15 font-mono text-[10px] uppercase tracking-widest text-orange-50/70"><tr><th className="py-3 pr-4">Term</th><th className="py-3 pr-4">Original listing</th><th className="py-3 pr-4">Proposal</th><th className="py-3">Result</th></tr></thead><tbody className="font-mono text-sm"><Row term="Amount" /><Row term="Loan currency" /><Row term="Annual rate" /><Row term="Duration" /><Row term="Collateral LTV" /></tbody></table></div><p className="mt-5 text-sm leading-6 text-orange-50">Accepting an exact match confirms the existing on-chain listing. Accepting changed terms cancels the public listing and creates a new private deal bound to both known wallets. The backend never mutates original terms in place.</p><div className="mt-6 flex flex-wrap gap-3"><Button disabled>Accept</Button><Button variant="secondary" disabled>Decline</Button><Button variant="secondary" disabled>Counter with structured terms</Button></div></Card>;
}
function Row({ term }: { term: string }) { return <tr className="border-b border-amber/10"><td className="py-4 pr-4 text-white">{term}</td><td className="py-4 pr-4 text-orange-50/60">—</td><td className="py-4 pr-4 text-orange-50/60">—</td><td className="py-4 text-orange-50/60">Unavailable</td></tr>; }
