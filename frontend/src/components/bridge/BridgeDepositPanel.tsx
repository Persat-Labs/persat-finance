"use client";
import { useState } from "react";
import { Button, Card, Modal } from "@/lib/design-system";

export function BridgeDepositPanel() { const [open, setOpen] = useState(false); return <><Card className="mt-8 max-w-3xl"><p className="eyebrow">Funding // Screen F1</p><h2 className="mt-3 font-display text-2xl uppercase">Deposit collateral</h2><p className="mt-3 leading-7 text-orange-50">The bridge health service must select the healthiest available path using pause status, observed success rate, and on-Solana liquidity. It cannot silently guess.</p><Button className="mt-7" onClick={() => setOpen(true)}>Review collateral bridge</Button></Card><Modal open={open} onClose={() => setOpen(false)} title="Collateral bridge"><p className="eyebrow">Bridge health: unavailable</p><p className="mt-4 leading-7 text-orange-50">Manual selection is shown only because the bridge-health service is not configured. No deposit can start until the approved bridge SDK, token mint, and program integration are verified.</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><BridgeChoice name="tBTC" detail="Threshold Network path" /><BridgeChoice name="zBTC" detail="Zeus BitcoinKit path" /></div><p className="mt-6 text-xs leading-5 text-orange-50/60">The selected route will later open an in-app modal. Persat never takes custody of Bitcoin or stablecoins.</p></Modal></>; }
function BridgeChoice({ name, detail }: { name: string; detail: string }) { return <button disabled className="min-h-24 border border-amber/20 bg-ink p-4 text-left opacity-60"><span className="font-display text-xl text-white">{name}</span><span className="mt-2 block font-mono text-[10px] uppercase tracking-widest text-orange-50">{detail}</span><span className="mt-3 block font-mono text-[10px] uppercase tracking-widest text-amber">Unavailable until verified</span></button>; }
